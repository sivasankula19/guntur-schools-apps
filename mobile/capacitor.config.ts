import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gunturschools',
  appName: 'Vyuham',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
